meta {
  name: createRental
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/rental
  body: json
  auth: none
}

body:json {
  {
    "bicycleId": "{{bicycleId}}"
  }
}

script:post-response {
  bru.setVar("rentalId", res.body.id);
}
