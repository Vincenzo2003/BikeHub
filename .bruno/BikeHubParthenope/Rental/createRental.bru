meta {
  name: createRental
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/rental
  body: json
  auth: inherit
}

body:json {
  {
    "bicycleId": "{{bicycleId}}"
  }
}

script:post-response {
  bru.setVar("rentalId", res.body.id);
}
